settings (
	java_package: "com.example.shell"
)

param => (user, int)
shell 400 (
       param => (executable, string)
       => 100 execute
       => 101 execute_with_argument (argument, string), (arg2, int), (arg3, ints)
)